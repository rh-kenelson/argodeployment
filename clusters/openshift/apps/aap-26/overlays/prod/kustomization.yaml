apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: aap
resources:
  - ../../base
  - secretstore-vault.yaml
  - externalsecret-aap-admin-password-prod.yaml
patches:
  - path: patch-aap-instance.yaml
    target:
      group: aap.ansible.com
      version: v1alpha1
      kind: AnsibleAutomationPlatform
      name: aap
